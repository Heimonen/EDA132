#include <utility>
using std::pair;
#include <vector>
using std::vector;
#include <string>

class Example {
	typedef pair<string, bool> Attribute;
	typedef vector<Attribute> Attributes;
public:

private:

};